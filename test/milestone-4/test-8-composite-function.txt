program CompositeFunction;

type
  Pair == record
    first: integer;
    second: integer
  end;

var
  result: Pair;

function makePair(a, b: integer): Pair;
begin
  makePair.first := a;
  makePair.second := b
end;

begin
  result := makePair(3, 4);
  writeln(result.first, result.second)
end.
